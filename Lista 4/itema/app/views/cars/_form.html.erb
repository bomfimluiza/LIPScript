<%= form_for @car do |f| %>
    <% if @car.errors.any? %>
        <div id="error_explanation">
        <h2>
            <%= pluralize(@car.errors.count, "error") %> prohibited
            this car from being saved:
        </h2>
        <ul>
            <% @car.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
            <% end %>
        </ul>
        </div>
    <% end %>
    
    <p>
        <%= f.label :plate %><br>
        <%= f.text_field :plate %>
    </p>
    <p>
        <%= f.label :cartype %><br>
        <%= f.select(:cartype, options_for_select([['Urban', 1], ['Motocross', 2], ['Luxury', 3]], 1)) %>
    </p>
    <p>
        <%= f.label :year %><br>
        <%= f.text_field :year %>
    </p>
   
    <p>
        <%= f.submit %>
    </p>
<% end %>